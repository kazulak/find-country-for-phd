{
  "name": "european-phd-country-match-platform",
  "version": "1.0.0",
  "description": "Validation, data engineering, and static deployment for the European PhD Country Match Platform",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run validate && npm run test && npm run data:build && npm run web:build && npm run data:build && npm run verify",
    "preview": "astro preview",
    "validate": "node scripts/validate.js",
    "data:build": "node scripts/build.js",
    "report": "node scripts/report.js",
    "test": "node scripts/test.js",
    "test:e2e": "playwright test",
    "web:build": "astro build",
    "verify": "node scripts/verify-output.js"
  },
  "dependencies": {
    "@astrojs/react": "^6.0.1",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "astro": "^7.0.7",
    "js-yaml": "^4.1.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "vite": "^5.0.0"
  }
}
