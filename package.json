{
  "name": "european-phd-country-match-platform",
  "version": "1.0.0",
  "description": "Validation, data engineering, and static deployment for the European PhD Country Match Platform",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run validate && npm run test && npm run web:build && npm run data:build && npm run verify",
    "preview": "vite preview",
    "validate": "node scripts/validate.js",
    "data:build": "node scripts/build.js",
    "report": "node scripts/report.js",
    "test": "node scripts/test.js",
    "web:build": "vite build",
    "verify": "node scripts/verify-output.js"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
